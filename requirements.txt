# Core (the paper's environment: Python 3.7, PyTorch 1.8.1, NumPy 1.21.5)
numpy>=1.21
torch>=1.8

# Optional: only needed by MarketPanel.from_long_dataframe and the scripts
pandas>=1.3
scipy>=1.7

# Tests
pytest>=7.0
